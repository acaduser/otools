(vl-load-com)
(if (>= (atof (getvar "acadver")) 21.0)
	(setq *start* (getvar "millisecs"))
	(setq *start* (getvar "date"))
)

(load "otools.lsp" nil)
;;(load "settings.lsp" nil)

(princ (strcat "\n" (findfile "acaddoc.lsp") ""))
(if (>= (atof (getvar "acadver")) 21.0)
	(princ (strcat " " (rtos (* (- (getvar "millisecs") *start*) 0.001) 2 2) "s"))
	(princ (strcat " " (rtos (* 60 60 24 (- (getvar "date") *start*)) 2 2) "s"))
)
(princ)
